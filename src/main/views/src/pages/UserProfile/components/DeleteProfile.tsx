import { Button } from "rsuite";

const DeleteProfile = () => {
    return (
        <Button appearance="primary" color="red">
            Delete user
        </Button>
    );
};

export default DeleteProfile;
